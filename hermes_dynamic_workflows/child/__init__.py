"""Standalone child-agent subsystem.

Runner, agent-type presets, worktree isolation, and the child-only
``structured_output`` tool. Bundled agent-type definitions live in ``defs/``.
Depends only on ``core`` (and ``storage``); never on ``engine`` or ``adapters``.
"""
